# If you change this, change in OktaFakeAlbumsServer also
server.port: 8081

# Okta setting. The server points to this uri to pull the JSON Web Keys. 
# The access tokens are coming in as JWT tokens
# spring.security.oauth2.resourceserver.jwt.jwk-set-uri: <PLACE OKTA ISSUER URL HERE FROM OKTA CONSOLE>/v1/keys
spring.security.oauth2.resourceserver.jwt.jwk-set-uri: https://dev-29262673.okta.com/oauth2/default/v1/keys

